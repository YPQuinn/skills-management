# Operator workflow prototype asset

The throwaway Appica prototype is preserved outside `main`:

- Branch: `prototype/operator-workflow`
- Commit: `9cbf486`
- Path: `.scratch/local-skill-manager-mvp/prototypes/operator-workflow/`
- Variants: Operations inbox, Resource explorer, Delivery plan

The prototype was validated with:

```text
npm run build
```

To inspect it without merging it into `main`, switch to the prototype branch and run:

```text
./.scratch/local-skill-manager-mvp/prototypes/operator-workflow/run.sh
```
